🧩 Git & GitHub CheatsheetHier sind die wichtigsten Git-Befehle für deinen Alltag in VS Code oder im Terminal – mit kurzen Erklärungen direkt daneben.```bashgit init                     # Neues lokales Git-Repository im aktuellen Ordner erstellengit status                   # Zeigt an, welche Dateien geändert, neu oder gelöscht wurdengit add .                    # Alle geänderten und neuen Dateien für den nächsten Commit vormerkengit add datei.py             # Nur eine bestimmte Datei vormerkengit commit -m "Beschreibung" # Änderungen mit einer Nachricht speichern (lokal, noch nicht auf GitHub)git branch -M main           # Haupt-Branch (Standard) auf "main" setzengit remote add origin https://github.com/benutzer/repo.git  # Lokales Repo mit GitHub verbindengit remote -v                # Zeigt alle Remote-URLs (fetch/push) angit fetch                    # Holt neue Daten (Commits, Branches) vom Remote-Repo, ändert aber NICHT deinen Codegit pull                     # Holt neue Daten UND integriert sie in deinen aktuellen Branch (fetch + merge)git push -u origin main      # Lokale Commits zum ersten Mal auf GitHub hochladengit push                     # Weitere Commits hochladen, nachdem Remote & Branch gesetzt sindgit log --oneline            # Zeigt alle Commits in Kurzform (ID + Nachricht)git clone https://github.com/benutzer/repo.git  # Ein bestehendes GitHub-Repo lokal klonen```---💡 Kurz erklärt: Fetch & Pull| Befehl | Wann verwenden | Wirkung ||---------|----------------|----------|| `git fetch` | Wenn du wissen willst, ob auf GitHub neue Änderungen existieren | Holt neue Infos, **ändert deinen Code nicht** || `git pull` | Wenn du die Änderungen auch in deinem lokalen Projekt haben willst | Holt + integriert die Änderungen (**fetch + merge**) |